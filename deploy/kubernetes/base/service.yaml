apiVersion: v1
kind: Service
metadata:
  name: asterdrive
  labels:
    app.kubernetes.io/name: asterdrive
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: asterdrive
  ports:
    - name: http
      appProtocol: http
      port: 80
      targetPort: http
