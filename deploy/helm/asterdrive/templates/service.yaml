apiVersion: v1
kind: Service
metadata:
  name: {{ include "asterdrive.fullname" . }}
  labels:
    {{- include "asterdrive.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "asterdrive.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      appProtocol: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
